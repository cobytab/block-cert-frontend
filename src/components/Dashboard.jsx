import React from "react";

// React strap
import { Row, Col } from "reactstrap";

import styled from "styled-components";

// Custom imports
import { colors, StyledContainer, Welcome } from "./shared";


const Dashboard = () => {
  return (
    <StyledContainer>
     <h1>Dashboard</h1>
    </StyledContainer>
  );
};

export default Dashboard;
